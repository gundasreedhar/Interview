package interview;

public class LengthOfLongestSubString {
	
	public static int lengthOfLongestSubString(String str)
	{
		int result = 0;
		boolean [] flag = new boolean [256];
		int start = 0;
		for(int i = 0 ; i < str.length() ; i++)
		{
			char ch = str.charAt(i);
			
			if(flag[ch])
			{
				result = Math.max(result,i-start);
				
				for(int k = start ; k < i ; k++)
				{
					// until the matching char occurs make the chars htat are befoere that matching char to flase
					// bcoz how ever the length will be less thn it is being computed.
					if()
				}
				
			}
			else
			{
				flag[ch] = true;
			}
		}
		
		
		return result;
	}

	public static void main(String[] args) {
		
		String s = "ACBBA EIBCXAPOQWRL"; //"softwar development in test developmentt";
		System.out.println("The length longest sub String is : " + lengthOfLongestSubString(s));
	}

}
